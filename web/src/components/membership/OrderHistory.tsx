'use client';

/**
 * Stub component — the order history / membership system has been removed.
 * Renders an empty state so the account page layout stays intact.
 */
export default function OrderHistory() {
  return (
    <div className="text-center py-8 text-sm text-muted-foreground">
      <p>Order history is currently unavailable.</p>
    </div>
  );
}
